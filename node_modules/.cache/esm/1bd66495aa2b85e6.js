let helpController,Signup;_aa2‍.x([["default",()=>_aa2‍.o]]);_aa2‍.w("../Controller/helpController",[["default",["helpController"],function(v){helpController=v}]]);_aa2‍.w("../model/Signup",[["default",["Signup"],function(v){Signup=v}]]);const jwt = require("jsonwebtoken");


// const config = process.env;
const SECRET_KEY= "MYSECRETKEY"

const auth = async(req, res, next) => {
  const token =
    req.headers["token"];
  if (!token) {
    return res.status(403).send("A token is required for authentication");
  }
  try {
    const decoded = await jwt.verify(token,SECRET_KEY);
    req.user = decoded;
  } catch (err) {
    return res.status(401).send("Invalid Token");
  }
  return next();
};

_aa2‍.d(auth);