let helpController,Signup;_de8‍.x([["default",()=>_de8‍.o]]);_de8‍.w("../Controller/helpController",[["default",["helpController"],function(v){helpController=v}]]);_de8‍.w("../model/Signup",[["default",["Signup"],function(v){Signup=v}]]);const jwt = require("jsonwebtoken");


// const config = process.env;
const SECRET_KEY= "MYSECRETKEY"

const auth = async(req, res, next) => {
  const token =
    req.headers["token"];
  if (!token) {
    return res.status(403).send("A token is required for authentication");
  }
  try {
    const decoded = await jwt.verify(token,SECRET_KEY);
    req.user = decoded;
  } catch (err) {
    return res.status(401).send("Invalid Token");
  }
  return next();
};

_de8‍.d(auth);