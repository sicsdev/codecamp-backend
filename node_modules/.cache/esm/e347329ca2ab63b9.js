let express,mongoose,DATABASE_URI,routes;_a2a‍.w("express",[["default",["express"],function(v){express=v}]]);_a2a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_a2a‍.w("./config",[["DATABASE_URI",["DATABASE_URI"],function(v){DATABASE_URI=v}]]);_a2a‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);


const app = express();

app.use(function (req, res, next) {
  let origin = req.headers.origin;
  res.header("Access-Control-Allow-Origin",req.headers.host.indexOf("localhost") > -1? "http://localhost:3000": origin);
  res.header("Access-Control-Allow-Methods", "GET,HEAD,OPTIONS,POST,PUT");
  res.header("Access-Control-Allow-Headers","Origin, X-Requested-With, Content-Type, Accept, Authorization");
  res.header("Access-Control-Allow-Credentials", true);
  next();
});



app.use(express.json());
app.use("/api", routes);
app.use(express.urlencoded({ extended: true }));

mongoose
  .connect(DATABASE_URI, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => app.listen(7000, () => console.log("listen on port 7000.")))
  .catch((error) => _a2a‍.g.console.log("error occured", error));
